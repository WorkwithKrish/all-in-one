// const { FaZ } = require("react-icons/fa6");

// const asyncFn = (item) => {
//   return new Promise((resolve, reject) =>
//     setTimeout(() => resolve(item * 2), 100)
//   );
// };
// const mapAsyncLimit = async (arr, limit, asyncFn) => {
//   if (limit < 1) return;
//   let response'
//    = [];
//   let start = 0;
// ÷ q  QZ :FaZ1 1  return async function () {Ssz
//     await asyncFn(arr.slice(start, start + limit));
//   };
// };

// mapAsyncLimit([1, 2, 3], 2, asyncFn);
